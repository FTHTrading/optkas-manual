# Release artifacts
This directory contains generated PDF exports of the OPTKAS Institutional Operating Manual.

## Generating

```bash
npm run build:pdf
```

Requires Puppeteer (`npm install puppeteer`).
